import React, { FC } from 'react'

const NavigationMenu: FC = () => {
    return (
        <nav>
            <div className={'nav-tab'}>신청 리뷰어(10)</div>
            <div className={'nav-tab'}>선정 리뷰어(0)</div>
        </nav>
    )
}

export default NavigationMenu
