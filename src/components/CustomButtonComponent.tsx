import React, { FC } from 'react'

const CustomButtonComponent: FC = () => {
    const customButtonClassName = 'custom-button'
    return <div className={customButtonClassName}>버튼</div>
}

export default CustomButtonComponent
